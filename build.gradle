plugins {
    id "com.bmuschko.nexus" version "2.3.1"
}

group GROUP
version VERSION

subprojects { subProject ->
    group GROUP
    version VERSION

    repositories {
        mavenCentral()
        jcenter()
        maven {
            url uri('localmaven')
        }
    }

    apply plugin: 'java'
    apply from: rootProject.file('gradle/dependencies.gradle')
    apply from: rootProject.file('gradle/push-artifacts.gradle')

    sourceCompatibility = 1.7
}
